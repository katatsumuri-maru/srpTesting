services:
  relay:
    build: ./relay
    container_name: relay
    environment:
      - RELAY_PORT=7001
    ports:
      - "443:443"
    networks:
      - internet

  agent:
    build: ./agent
    container_name: agent
    environment:
      - RELAY=relay:7001
      - ENDPOINT=web:443
    depends_on:
      - relay
      - web
    networks:
      - internet
      - home

  web:
    image: caddy
    container_name: web
    volumes:
    - ./Caddyfile:/etc/caddy/Caddyfile:ro
    networks:
    - home

networks:
  internet:
  home:
